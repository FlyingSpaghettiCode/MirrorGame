This is where we will keep level files by default.  We can also write utility files for traits that will be reused throughout many levels
and include them within another level using the "include" command.
